import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.AppHBT',
  appName: 'AppHBT',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
